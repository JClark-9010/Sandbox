using System;

public class GitDemo
{
	public GitDemo()
	{
		string add = "Add changes to saved file";
		string commit = "Commit changes to staging area";
		string pull = "Pull any repo changes to local branch";
		string push = "Push commited changes to repo";
		string merge = "Merge local branch with other branch";
	}
}
